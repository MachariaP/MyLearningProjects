#!/usr/bin/python3

'''
    a list - a collection of items in a prticular order
    ([]) - a square brackets indicates a list.
    Elements are separated by commas.

'''

bicycles = ['trek', 'cannondale', 'redline', 'specialized']
print(bicycles)

print()
# Accessing Elements in a list
''' 
    To access an element in a list:
        write name of the list
        followed by the indexof the item enclosed in square brackets
    example below:

'''

print(bicycles[0])
print(bicycles[3])
